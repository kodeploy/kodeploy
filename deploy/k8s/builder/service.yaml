apiVersion: v1
kind: Service
metadata:
  name: kodeploy-builder
  namespace: default
spec:
  selector:
    app: kodeploy-builder
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP                                        # 클러스터 내부 전용 - core만 부른다 (HTTPRoute 없음)
